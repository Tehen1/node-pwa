{
  "name": "studio-fixierun-ai-coach",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "next build && next export",
    "postexport:sw": "node scripts/generate-sw.js",
    "deploy:gh-pages": "DEPLOY_TARGET=github-pages npm run export && npm run postexport:sw && npx gh-pages -d out -b gh-pages"
  },
  "dependencies": {
    "next": "15.x",
    "react": "18.x",
    "react-dom": "18.x"
  },
  "devDependencies": {
    "workbox-build": "^6.5.4",
    "gh-pages": "^5.0.0"
  }
}